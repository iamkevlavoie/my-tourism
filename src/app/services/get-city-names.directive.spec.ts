import { GetCityNamesDirective } from './get-city-names.directive';

describe('GetCityNamesDirective', () => {
  it('should create an instance', () => {
    const directive = new GetCityNamesDirective();
    expect(directive).toBeTruthy();
  });
});
